---
title: "Service↔API 스키마 의존을 끊어 순환 import를 제거하다 (Clean Architecture) — 5월 24일 개발로그"
excerpt: "smahn9123이 Orange-RestAPI의 Service 계층이 상위 API 스키마를 알지 못하도록 infra/cgroup·Organization·CommandTemplate 세 도메인을 같은 패턴으로 분리하고, TYPE_CHECKING으로 우회하던 순환 import를 매퍼 도입으로 원천 제거한 하루."
category: dev-log
date: 2026-05-24
author: Orange Labs
tags: [일간, 백엔드, smahn9123]
---

5월 24일(일), 활동 멤버 1명이 총 3커밋, +299/−96 라인. smahn9123이 **Clean Architecture 의존 방향 정리(OR-1425)**를 Orange-RestAPI 세 도메인에 같은 패턴으로 진행했다.

```
멤버     커밋   +라인    −라인
──────────────────────────────
smahn9123     3      299       96
```

## smahn9123

핵심은 "Service 계층이 위 계층(API)의 스키마를 알지 못하게" 분리하는 것. 세 도메인을 차례로:

- **infra/cgroup** (`d961628`, +63/−7) — `app/infra/schemas/cgroup.py`를 신설(ContainerCpuStats/MemoryStats)하고, API 변환은 새 `usecases/mapper/health.py`가 담당. Infra가 API를 import하던 역방향 의존을 제거.

- **OrganizationService** (`a9586b3`, +112/−18) — `services/schemas/organization.py`에 전용 Input/Result를 추가. 응답 메시지 빌드는 새 `api/v3/mapper/organization.py`로 이관.

- **CommandTemplate** (`1b5295c`, +124/−71) — `services/schemas/command_template.py` 신설. 기존 `TYPE_CHECKING` + `from __future__ import annotations`로 우회하던 **순환 import를 원천 제거**. `create_one` 시그니처를 keyword 인자 6개에서 단일 payload 객체로 단순화.

세 도메인 모두 "Service 전용 Input 스키마 + UseCase 매퍼"라는 동일한 구조로 정리해, 계층 간 의존이 한 방향(API → Service → Infra)으로만 흐르도록 만들었다.

---

*이 글은 오렌지랩스 개발팀의 자동 생성 개발로그입니다.*
